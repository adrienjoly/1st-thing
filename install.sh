# from : https://help.github.com/articles/using-jekyll-with-pages/
ruby --version           # must be at least 2.0
gem install bundler      # installs bundle. may need to be run as root (sudo)
bundle install           # runs Gemfile => installs github-pages
# gem update github-pages
# gem install jekyll
